<section class="view armor">

    {{!-- Header Row --}}
    <li class="item flexrow items-header">
        <div class="item-image"></div>
        <div class="item-name">{{localize "WW.Item.Name"}}</div>
    
        <div class="flex-width-60">{{localize "WW.Armor.Type"}}</div>
        <div class="item-stat">{{localize "WW.Defense.Label"}}</div>
        <div class="flex-width-60">{{localize "WW.Equipment.Availability.Label"}}</div>
        <div class="flex-width-60">{{localize "WW.Equipment.Price"}}</div>
        <div class="item-last">{{localize "WW.Item.Description"}}</div>
    </li>
    
    {{!-- Rows --}}
    <ol class="item-list armor-list">
        {{#each documents as |doc id|}}
        <li class="item flexrow flex-group-left" data-item-id="{{doc._id}}">
            {{!-- Icon --}}
            <div class="item-image"><img src="{{doc.img}}" data-tooltip="{{doc.name}}" width="24" height="24" /></div>

            {{!-- Name --}}
            <div class="item-name">
                <a data-action="openSheet" data-item-uuid="{{doc.uuid}}" data-tooltip="{{doc.tooltip}}">{{doc.name}}</a>
            </div>

            <div class="flex-width-60">{{ doc.armorTypeLabel }}</div>
            <div class="item-stat">{{ doc.defenseLabel }}</div>
            <div class="flex-width-60">{{ doc.availabilityLabel }}</div>
            <div class="flex-width-60">{{{ doc.priceLabel }}}</div>

            {{!-- Description --}}
            <div class="item-last">{{{ doc.system.descriptionEnriched }}}</div>
        </li>
        {{/each}}
    </ol>

</section>