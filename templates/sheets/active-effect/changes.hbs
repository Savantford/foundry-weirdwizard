  <!-- Changes Tab of WWActiveEffectConfig -->
<section class="tab changes{{#if tab.active}} active{{/if}}" data-group="{{tab.group}}" data-tab="{{tab.id}}">
    <header class="effect-change effects-header flex flexrow flex-nowrap">
        <div class="key flexlarge">{{ localize "WW.Effect.ToApply" }}</div>
        <div class="value">{{ localize "WW.Effect.Value" }}</div>
        <div class="controls effect-controls flexshrink">
            <button class="inline-control icon fa-regular fa-square-plus" data-action="addChange"></button>
        </div>
    </header>
    <ol class="scrollable changes-list flex flex-nowrap" data-changes>
      {{#each source.changes as |change i|}}
        <li class="effect-change flexrow flex-between" data-index="{{i}}">
            <div class="key flexlarge">
                <select name="changes.{{i}}.key" style="width:100%;">
                    <option label="No Effect"></option>
                {{#each @root.effectChangeOptions as |category i|}}
                    <optgroup label="{{localize category.header}}">
                        {{selectOptions category.options selected=change.key localize=true}}
                    </optgroup>
                {{/each}}
                </select>
            </div>

            <div class="value">
                {{!-- {{formInput changeFields.value name="changes.{{ i }}.value" value=change.value}} --}}
              {{#if (eq change.valueType "int")}}
                <input type="number" name="changes.{{i}}.value" value="{{change.value}}" min="0"/>
              {{else if (eq change.valueType "str")}}
                <input type="text" name="changes.{{i}}.value" value="{{change.value}}"/>
              {{else if (eq change.valueType "boo")}}
                <input type="checkbox" name="changes.{{i}}.value" {{checked change.value}}>
              {{else}}
                <input style="display: none;" type="text" name="changes.{{i}}.value" value="{{change.value}}"/>
              {{/if}}
            </div>
            
            <div class="controls effect-controls flexshrink">
                <button type="button" class="effect-control inline-control icon fa-solid fa-trash" data-action="deleteChange"></button>
            </div>
        </li>
      {{/each}}
    </ol>

</section>