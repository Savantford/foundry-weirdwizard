<div>
  <section class="header-section weirdwizard">

    {{!-- Right Banner --}}
    <header class="sheet-banner-wrapper">
      <div class="type">{{ localize "WW.CharOptions.Ancestry" }}</div>

      <div class="icon-frame">
        <img src="{{ document.src }}" data-edit="src" data-tooltip="WW.System.ViewImage" />
      </div>
    </header>
    
    {{#if title.show}}
    {{> "systems/weirdwizard/templates/journal/parts/journal-page-header.hbs" this=this }}
    {{/if}}

    {{!-- Page Content (Editor/Description) --}}
    <section class="journal-page-content">
      {{{editor.content}}}
    </section>

  </section>
  
  {{!-- Benefits Section --}}
  <section class="benefits-section weirdwizard">

    {{!-- Benefit Block --}}

    <div class="benefit-block">
  
      {{!-- Heading --}}
      <h3>{{ traitsHeading }}</h3>
  
      {{!-- Descriptor(s) --}}
      {{#if listEntries.benefit1.descriptors.length}}
      <ol class="stat-inline list-entries">
  
        <label>{{localize "WW.ListEntry.Descriptor.Label"}}:</label>
  
        {{#each listEntries.benefit1.descriptors as |entry|}}
        <li class="list-entry">
          <a data-tooltip="{{entry.desc}}">{{entry.name}}</a>
        </li>
        {{/each}}
  
      </ol>
      {{/if}}
  
      {{!-- Senses --}}
      {{#if listEntries.benefit1.senses.length}}
      <ol class="stat-inline list-entries">
        <label>{{localize "WW.ListEntry.Sense.Label"}}:</label>
  
        {{#each listEntries.benefit1.senses as |entry|}}
        <li class="list-entry">
          <a data-tooltip="{{entry.desc}}">{{entry.name}}</a>
        </li>
        {{/each}}
  
      </ol>
      {{/if}}
  
      {{!-- Bonus Languages --}}
      {{#if listEntries.benefit1.languages.length}}
      <ol class="stat-inline list-entries">
        <label>{{localize "WW.ListEntry.Language.Bonus"}}:</label>
  
        {{#each listEntries.benefit1.languages as |entry|}}
        <li class="list-entry">
          <a data-tooltip="{{entry.desc}}">{{entry.name}}</a>
        </li>
        {{/each}}
  
      </ol>
      {{/if}}
  
      {{!-- Attributes --}}
      {{#if benefits.benefit1.attributes}}
      <div class="stat-inline">
        <label>{{localize "WW.Attributes.Label"}}:</label>
  
        {{benefits.benefit1.attributes}}
      </div>
      {{/if}}
  
      {{!-- Natural Defense, Health --}}
      {{#if (or benefits.benefit1.stats.naturalIncrease benefits.benefit1.stats.healthIncrease)}}
      <div class="stat-inline flex-wrap flex-children">
        
        {{#if benefits.benefit1.stats.naturalIncrease}}
        <div>
          <label>{{localize "WW.Defense.Natural"}}:</label>

          +{{benefits.benefit1.stats.naturalIncrease}}
  
        </div>
        {{/if}}

        {{#if benefits.benefit1.stats.healthIncrease}}
        <div>
          <label>{{localize "WW.Health.Label"}}:</label>
  
          +{{benefits.benefit1.stats.healthIncrease}}
  
        </div>
        {{/if}}
  
      </div>
      {{/if}}
  
      {{!-- Size, Speed --}}
      <ol class="stat-inline list-entries movement-traits">
  
        {{!-- Size --}}
        <label>{{localize "WW.Stats.Size"}}:</label>
  
        <span>{{ benefits.benefit1.sizeDisplayed }}</span>
  
        {{!-- Speed --}}
        <label>{{localize "WW.Stats.Speed"}}:</label>
  
        <span>{{benefits.benefit1.stats.speedNormal}}</span>
  
        {{!-- Movement Traits --}}
        {{#each listEntries.benefit1.movementTraits as |entry|}}
        <li class="list-entry">
          <a data-tooltip="{{entry.desc}}">{{entry.name}}</a>
        </li>
        {{/each}}
  
      </ol>
  
      {{!-- Immunities --}}
      {{#if listEntries.benefit1.immunities.length}}
      <ol class="stat-inline list-entries">
        <label>{{localize "WW.ListEntry.Immunity.Label"}}:</label>
  
        {{#each listEntries.benefit1.immunities as |entry|}}
        <li class="list-entry">
          <a data-tooltip="{{entry.desc}}">{{entry.name}}</a>
        </li>
        {{/each}}
  
      </ol>
      {{/if}}
  
      {{!-- Item List Area --}}
      {{#if benefits.benefit1.itemsInfo}}
      <ol class="described-list items-area benefit1">

        {{!-- Populate Items --}}
        {{#each benefits.benefit1.itemsInfo as |item id|}}
        <li class="item-reference item {{#if item.missing}}missing{{/if}}" data-item-uuid="{{item.uuid}}">
          <label><img src="{{item.img}}"> {{item.name}}:</label>
  
          <span>{{{item.description}}}</span>
        </li>
        {{/each}}

      </ol>
      {{/if}}
  
    </div>

  </section>

</div>